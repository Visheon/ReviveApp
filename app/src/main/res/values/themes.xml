<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReviveApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ReviveApp" parent="Base.Theme.ReviveApp" />


    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/darkGreen</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreenicon</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.ReviveApp</item>
    </style>
</resources>